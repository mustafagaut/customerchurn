# customerchurn
Customer Churn Prediction using the Telco dataset. This project builds an end-to-end ML pipeline with data cleaning, EDA, feature engineering, and model training using Logistic Regression, Random Forest, and XGBoost. Includes handling categorical data, class imbalance, model evaluation (AUC, F1),.
